(load-file "core/straight.el")

;; Install fonts required by all-the-icons.el
(require 'cl-lib)
;; (straight-use-package 'all-the-icons)
;; (let ((window-system (cl-case system-type
;;                        (darwin 'ns)
;;                        (windows-nt 'w32)
;;                        (t 'x))))
;;   (all-the-icons-install-fonts t))
