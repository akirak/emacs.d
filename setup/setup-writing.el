;;;; Quotation 
(use-package typo)

;;;; Counting words
(use-package wc-mode
  ;; TODO: Configure modelines
  )

(use-package org-wc)

;;;; Writegood
(use-package writegood-mode)

(provide 'setup-writing)
